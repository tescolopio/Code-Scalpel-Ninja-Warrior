# Code Scalpel Change Budget Configuration
# Relaxed profile intended for Stages 1–4.

version: "1.0.0"

budgets:
  max_files_modified: 50
  max_files_created: 25
  max_files_deleted: 5

  max_lines_added: 5000
  max_lines_deleted: 5000
  max_lines_modified: 5000

  max_total_line_changes: 20000

  # Keep the knobs present but generous in relaxed mode.
  max_cumulative_security_risk_score: 500
  require_review_threshold: 9999

reset:
  mode: "session"

exemptions:
  - "tests/**/*.py"
  - "docs/**/*.md"
